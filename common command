#Save file denied 
sudo chown -R $USER /vuejs/ninjatutorials
# Copy from cureent directory to destination 
cp -R ./ /srv/ 
#Making a file writable
Chmod s+u filename
#Root directory sudo -i
#for ipconfig  

Install net-tools


## unzip 
'``

1574
down vote
accepted
If the unzip command isn't already installed on your system, then run:

sudo apt-get install unzip
After installing the unzip utility, if you want to extract to a particular destination folder, you can use:

unzip file.zip -d destination_folder


sudo openconnect vpn.tu-berlin.de
```


move all files to subfolder 
 mv --force  ./* /vuetifychartjsproject



